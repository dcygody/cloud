spring:
  application:
    name: cn.zing.order.redis
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        # 自定义配置文件
        shared-configs:
          - data-id: dcy.common.properties
            refresh: true